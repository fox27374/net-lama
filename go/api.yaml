openapi: "3.0.0"
info:
  version: 1.0.0
  title: Minimal ping API server
paths:
  /netdata:
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/netData'
components:
  schemas:
    netData:
      type: object
      properties:
        name:
          type: string
        country:
          type: string
        latency:
          type: number
          format: double
        dlspeed:
          type: number
          format: double
        ulspeed:
          type: number
          format: double
        userip:
          type: string
        userisp:
          type: string
