# Speedtest client
Meassures the current internet speed and sends the results to the MQTT data topic.

Initial state: stop
Config section:
```
"SpeedTest": {
        "intervalSeconds": "120"
    }
```